# Task 9H RED evidence

## Implementation RED

A mutation replaced the listener's socket cleanup with a no-op. `ephemeral_listener_accepts_one_frame_and_cleans_its_path` failed because the temporary socket remained (`/tmp/task9h-red-cleanup.log`, exit 101). Restoring cleanup made the focused listener tests pass.

## Review-driven RED

The first fresh review `bb738859-db6b-46f9-904e-cd7ec7da4b21` returned BLOCK for EOF-dependent completion, unsafe pathname cleanup, and missing listener-level malformed-frame cleanup tests. To retain behavioral evidence for the first finding, a mutation changed the listener back to `read_to_end`; `ephemeral_listener_does_not_wait_for_half_close` failed with `RecvTimeoutError::Timeout` (`/tmp/task9h-red-half-close.log`, exit 101). The listener was then changed to read the declared prefix/payload, perform a nonblocking trailing-byte check, require a private mode-0700 parent, and verify the bound socket inode/device before cleanup. Focused listener tests now cover valid half-close-free delivery, wrong-token/truncated/trailing refusal cleanup, shared-parent refusal, and pre-existing-path preservation.

No daemon, persistent listener, TLS/mTLS, remote host, native process, backup/restore, fencing, route publication, deployment, or disruptive effect ran.
