# Task 9M RED evidence

A mutation changed valid response mapping in `LocalActionAdapter` to return `ActionAdapterError::Uncertain` instead of the executor's `FailedSafe` outcome. `local_action_adapter_preserves_outcome_and_uncertainty` then failed with `Err(Uncertain)` instead of `Ok(FailedSafe)` (`/tmp/task9m-red-adapter.log`, exit 101). Restoring outcome preservation made the 20 focused local transport/adapter tests pass (`/tmp/task9m-green-final-focused.log`).

Failure history is retained accurately: `/tmp/task9m-full-test.log` recorded the local listener `NotConnected` test failure; `/tmp/task9m-full-test-final2.log` recorded the dashboard `account setup failed: account refused` failure; the dashboard package rerun passed at `/tmp/task9m-dashboard-rerun.log`; and the final sequential full suite passed at `/tmp/task9m-full-test-final3.log`. The local listener test shutdown was made race-tolerant because the one-shot listener may close before the client half-close call. No normal-controller wiring, production call site, default/native executor, daemon, service unit, TCP/HTTP/TLS/mTLS, credential provisioning, remote host, process, backup/restore, fencing, route publication, deployment, or disruptive effect ran.
